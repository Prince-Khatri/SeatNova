info:
  name: searchMovie
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8081/movies/search?title=upad
  params:
    - name: title
      value: upad
      type: query
    - name: status
      value: RUNNING
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
